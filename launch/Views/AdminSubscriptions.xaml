<Page x:Class="launch.Views.AdminSubscriptions"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:launch.Views"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="880"
      Title="AdminSubscriptions">

    <Grid>
        <Grid Background="{StaticResource BG}" Panel.ZIndex="0">
            <Grid.Effect>
                <BlurEffect Radius="5"/>
            </Grid.Effect>
        </Grid>

        <Border Background="#8B2B8B" Opacity="0.5" Margin="80 120 80 80"/>
        <StackPanel>
            <StackPanel Margin="80 60 90 0" HorizontalAlignment="Left" Orientation="Horizontal">
                <RadioButton IsChecked="True" Click="btnAddSub_Click" Name="btnAddSub"  Content="добавить" Style="{StaticResource radioBtn}" Width="200" Height="50" />
                <RadioButton Name="btnViewSub" Click="btnViewSub_Click" Content="редактировать" Style="{StaticResource radioBtn}" Width="220" Height="50" Margin="20 0 0 0"/>
            </StackPanel>
            <Grid>
                <StackPanel Name="addSubPage" Margin="100 30 100 0" Visibility="Visible">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="410"/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <StackPanel Orientation="Horizontal" Margin="0 5 0 0">
                                <TextBlock Text="Название: " Style="{StaticResource BtnText}" HorizontalAlignment="Left"/>
                                <TextBox Name="newNameTB" Width="270" Style="{StaticResource TextBox}" Margin="10 0 0 0" TextChanged="newNameTB_TextChanged"/>
                            </StackPanel>
                            <StackPanel>
                                <TextBlock Text="Описание: " Style="{StaticResource BtnText}"/>
                                <TextBox Name="newDescTB" MaxLength="250" Height="100" TextWrapping="Wrap" Style="{StaticResource TextBox}" TextChanged="newDescTB_TextChanged"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0 15 0 0">
                                <TextBlock Text="Цена подписки: " Style="{StaticResource BtnText}" HorizontalAlignment="Left"/>
                                <TextBox Name="newPriceTB" Width="200" Style="{StaticResource TextBox}" Margin="10 0 0 70" TextChanged="newPriceTB_TextChanged"/>
                            </StackPanel>
                            <Button Name="btnAdd" Click="btnAdd_Click" Width="400" Height="60" Content="добавить" Style="{StaticResource ImageButton}" />
                        </StackPanel>
                        <StackPanel Grid.Column="1">
                            <TextBlock Margin="35 0 0 5" Text="превью: " FontSize="34" Style="{StaticResource BtnText}"/>
                            <Grid Width="210" Height="300" Background="#FF4C1C64">
                                <Image Source="Images/StarNether.png" Opacity="0.3" Margin="20,20,20,100"/>
                                <TextBlock Name="NameSub" Text="название" Width="190" Style="{StaticResource BtnText}" HorizontalAlignment="Center" VerticalAlignment="Top" Height="30" Margin="0 25 0 0" FontSize="36" TextAlignment="Center"/>
                                <TextBlock Name="DescriptionSub" Text="описание" Width="190" TextWrapping="Wrap" Style="{StaticResource BtnText}" TextAlignment="Center" Margin="0 65 0 110"/>
                                <TextBlock Name="PriceSub" Text="стоимость" Width="160" TextWrapping="Wrap" Style="{StaticResource BtnText}" TextAlignment="Center" Margin="0 195 0 0"/>
                                <Button Name="btnSub" Width="170" Content="оформить" Style="{StaticResource ImageButton}" VerticalAlignment="Bottom" Margin="0 0 0 20" IsEnabled="False"/>
                            </Grid>
                        </StackPanel>
                    </Grid>
                </StackPanel>
                <StackPanel Name="viewSubPage" Margin="100 30 100 0" Visibility="Hidden">
                    <ScrollViewer  HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden" PreviewMouseWheel="ScrollViewer_PreviewMouseWheel">
                        <StackPanel Orientation="Horizontal" Name="SPSubscriptions">

                        </StackPanel>
                    </ScrollViewer>
                </StackPanel>
            </Grid>
        </StackPanel>
    </Grid>
</Page>
