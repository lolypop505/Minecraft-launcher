<Window x:Class="launch.AdminWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:launch"
        xmlns:vm="clr-namespace:launch.Views.ViewModels"
        mc:Ignorable="d" Height="600" Width="1000" 
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">
    
    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120" />
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Frame x:Name="frame" Grid.Column="1" NavigationUIVisibility="Hidden" Content="{Binding CurrentAdminPage}"/>

        <StackPanel Background="Transparent" Grid.Column="1" Width="900" Height="50" VerticalAlignment="Top" HorizontalAlignment="Left" MouseDown="StackPanel_MouseDown"/>
        <StackPanel Grid.Column="1" Width="100" Height="60" Orientation="Horizontal" VerticalAlignment="Top" HorizontalAlignment="Right">
            <Button Style="{StaticResource ImageBtn}" Template="{StaticResource btn}" Width="50" Name="Collapse" Click="Collapse_Click">
                <Button.Resources>
                    <ImageSource x:Key="Img">Views/Images/BtnCollapse.png</ImageSource>
                    <ImageSource x:Key="ImgTrig">Views/Images/BtnCollapseTrig.png</ImageSource>
                </Button.Resources>
            </Button>
            <Button Style="{StaticResource ImageBtn}" Template="{StaticResource btn}" Width="50" Name="Clouze" Click="Clouze_Click">
                <Button.Resources>
                    <ImageSource x:Key="Img">Views/Images/BtnExit.png</ImageSource>
                    <ImageSource x:Key="ImgTrig">Views/Images/BtnExitTrig.png</ImageSource>
                </Button.Resources>
            </Button>
        </StackPanel>

        <Grid>
            <Grid.Background>
                <SolidColorBrush Color="#8B2B8B" Opacity="0.4"/>
            </Grid.Background>
            <StackPanel VerticalAlignment="Center">
                <Button Margin="0 0" Height="80" Style="{StaticResource ImageBtn}" Template="{StaticResource btn}" Command="{Binding OpenAdminHomePage}">
                    <Button.ToolTip>
                        <ToolTip Placement="Left" PlacementRectangle="0,0,100,80" VerticalOffset="20" Background="#8B2B8B" BorderBrush="White" BorderThickness="4">
                            <TextBlock Text="главная" Style="{StaticResource BtnText}"/>
                        </ToolTip>
                    </Button.ToolTip>
                    <Button.Resources>
                        <ImageSource x:Key="Img">Views/Images/MenuBtnHome.png</ImageSource>
                        <ImageSource x:Key="ImgTrig">Views/Images/MenuBtnHomeTrig.png</ImageSource>
                    </Button.Resources>
                </Button>
                <Button Margin="0 0" Height="80" Style="{StaticResource ImageBtn}" Template="{StaticResource btn}" Command="{Binding OpenAdminSubscriptionsPage}">
                    <Button.ToolTip>
                        <ToolTip Placement="Left" PlacementRectangle="0,0,100,80" VerticalOffset="20" Background="#8B2B8B" BorderBrush="White" BorderThickness="4">
                            <TextBlock Text="подписки" Style="{StaticResource BtnText}"/>
                        </ToolTip>
                    </Button.ToolTip>
                    <Button.Resources>
                        <ImageSource x:Key="Img">Views/Images/MenuBtnSub.png</ImageSource>
                        <ImageSource x:Key="ImgTrig">Views/Images/MenuBtnSubTrig.png</ImageSource>
                    </Button.Resources>
                </Button>
                <Button Margin="0 0" Height="80" Style="{StaticResource ImageBtn}" Template="{StaticResource btn}" Command="{Binding OpenAdminDownloadsPage}">
                    <Button.ToolTip>
                        <ToolTip Placement="Left" PlacementRectangle="0,0,100,80" VerticalOffset="20" Background="#8B2B8B" BorderBrush="White" BorderThickness="4">
                            <TextBlock Text="сборки" Style="{StaticResource BtnText}"/>
                        </ToolTip>
                    </Button.ToolTip>
                    <Button.Resources>
                        <ImageSource x:Key="Img">Views/Images/MenuBtnAssembly.png</ImageSource>
                        <ImageSource x:Key="ImgTrig">Views/Images/MenuBtnAssemblyTrig.png</ImageSource>
                    </Button.Resources>
                </Button>
                <Button Margin="0 0" Height="80" Style="{StaticResource ImageBtn}" Template="{StaticResource btn}" Command="{Binding OpenAdminNewsPage}">
                    <Button.ToolTip>
                        <ToolTip Placement="Left" PlacementRectangle="0,0,100,80" VerticalOffset="20" Background="#8B2B8B" BorderBrush="White" BorderThickness="4">
                            <TextBlock Text="новости" Style="{StaticResource BtnText}"/>
                        </ToolTip>
                    </Button.ToolTip>
                    <Button.Resources>
                        <ImageSource x:Key="Img">Views/Images/MenuBtnNews.png</ImageSource>
                        <ImageSource x:Key="ImgTrig">Views/Images/MenuBtnNewsTrig.png</ImageSource>
                    </Button.Resources>
                </Button>
                <Button Margin="0 0" Height="80" Style="{StaticResource ImageBtn}" Template="{StaticResource btn}" Command="{Binding OpenAdminUsersPage}">
                    <Button.ToolTip>
                        <ToolTip Placement="Left" PlacementRectangle="0,0,100,80" VerticalOffset="20" Background="#8B2B8B" BorderBrush="White" BorderThickness="4">
                            <TextBlock Text="пользователи" Style="{StaticResource BtnText}"/>
                        </ToolTip>
                    </Button.ToolTip>
                    <Button.Resources>
                        <ImageSource x:Key="Img">Views/Images/MenuBtnUsers.png</ImageSource>
                        <ImageSource x:Key="ImgTrig">Views/Images/MenuBtnUsersTrig.png</ImageSource>
                    </Button.Resources>
                </Button>
                <Button Margin="0 0" Height="80" Style="{StaticResource ImageBtn}" Template="{StaticResource btn}" Command="{Binding OpenAdminHelpdeskPage}">
                    <Button.ToolTip>
                        <ToolTip Placement="Left" PlacementRectangle="0,0,100,80" VerticalOffset="20" Background="#8B2B8B" BorderBrush="White" BorderThickness="4">
                            <TextBlock Text="сл. поддержки" Style="{StaticResource BtnText}"/>
                        </ToolTip>
                    </Button.ToolTip>
                    <Button.Resources>
                        <ImageSource x:Key="Img">Views/Images/MenuBtnHelpdesk.png</ImageSource>
                        <ImageSource x:Key="ImgTrig">Views/Images/MenuBtnHelpdeskTrig.png</ImageSource>
                    </Button.Resources>
                </Button>
                <Button Margin="0 0" Height="80" Style="{StaticResource ImageBtn}" Template="{StaticResource btn}" Name="Exit" Click="Exit_Click">
                    <Button.ToolTip>
                        <ToolTip Placement="Left" PlacementRectangle="0,0,100,80" VerticalOffset="20" Background="#8B2B8B" BorderBrush="White" BorderThickness="4">
                            <TextBlock Text="выход" Style="{StaticResource BtnText}"/>
                        </ToolTip>
                    </Button.ToolTip>
                    <Button.Resources>
                        <ImageSource x:Key="Img">Views/Images/menuBtnExit.png</ImageSource>
                        <ImageSource x:Key="ImgTrig">Views/Images/menuBtnExitTrig.png</ImageSource>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
